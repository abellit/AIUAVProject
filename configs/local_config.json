{
  "training": {
    "batch_size": 32,
    "learning_rate": 0.001,
    "max_episodes": 100
  },
  "paths": {
    "model_weights": "data/model_weights",
    "training_data": "data/training_data"
  }
}
